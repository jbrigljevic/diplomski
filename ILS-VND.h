#pragma once

#include<bits/stdc++.h>
#include "graph.h"

void initialize(std::set<int>* set){

}

void algorithm(Graph* graph, int max_iter, int c1, int c2, int c3, int c4){
    std::set<int> ind_set{};
    initialize(&ind_set);
}